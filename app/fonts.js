import { Inter, Rubik, Josefin_Sans } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'], display:'swap' })
export const rubik = Rubik({ subsets: ['latin'], display:'swap', variable:'--rubik'})
export const jose = Josefin_Sans({ subsets: ['latin'], display:'swap', variable:'--jose'})